<div id="clearance" class="sign-in">
  <h2><%= t(".title") %></h2>
  <div class="sign-in-flash" id="flash">
  <% flash.each do |key, value| %>
    <div class="flash <%= key %>"><%= value %></div>
  <% end %>
  </div>
  <div class="form">
    <%= render partial: '/sessions/form' %>
  </div>
</div>
