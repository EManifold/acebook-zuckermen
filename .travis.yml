language: ruby
rvm:
  - 2.5.0
  - jruby

env:
  - RACK_ENV='test'
  - DB=postgres

script:
  - bundle install - jobs=3 - retry=3
  - bundle exec rake db:create
  - bundle exec rake db:migrate
  - bundle exec rspec
